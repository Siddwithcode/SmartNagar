# Development: Vite proxy forwards /api to localhost:5000 (see vite.config.js)
VITE_API_URL=/api

# Production (split deploy): set to your backend URL, e.g. https://api.example.com/api
# Production (same domain / Docker): keep as /api
